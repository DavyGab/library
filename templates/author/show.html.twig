{% extends 'base.html.twig' %}

{% set menu = 'author' %}

{% block title %}Auteur{% endblock %}

{% block body %}
    <h1>Auteur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ author.id }}</td>
            </tr>
            <tr>
                <th>First_name</th>
                <td>{{ author.firstName }}</td>
            </tr>
            <tr>
                <th>Last_name</th>
                <td>{{ author.lastName }}</td>
            </tr>
            <tr>
                <th>Nationality</th>
                <td>{{ author.nationality }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('author_index') }}">Retour à la liste</a>

    <a href="{{ path('author_edit', {'id': author.id}) }}">Modifier</a>

    {{ include('author/_delete_form.html.twig') }}
{% endblock %}
